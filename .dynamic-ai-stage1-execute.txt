MEDBOT STAGE 1 — EXECUTION ONLY.

You are using opencode/big-pickle.

The previous Stage 1 attempt already:
- inspected the project
- confirmed the ai_registry schema
- confirmed exactly 2 duplicate groups
- created a valid backup
- made NO source/database changes

DO NOT repeat broad exploration.
DO NOT inspect unrelated files.
DO NOT inspect OpenCode auth files.
DO NOT start Stage 2.

CURRENT DATABASE:
~/MEDBOT/medbot_v2.sqlite3

CURRENT DUPLICATES:
1. google_gemini | gemini-flash-lite-latest | https://generativelanguage.googleapis.com/v1beta/models/gemini-flash-lite-latest:generateContent
   3 rows
2. google_gemini | gemma-4-26b-a4b-it | https://generativelanguage.googleapis.com/v1beta/models/gemma-4-26b-a4b-it:generateContent
   3 rows

TASK:

1. Inspect ONLY the existing registry-related functions in database.py and their callers in ai_router.py / ai_discovery.py.

2. Implement one canonical idempotent registry registration/upsert mechanism using:
   (provider, model, endpoint)

3. Before creating the unique index, merge the existing duplicate rows safely.
   Preserve useful non-null fields and historical:
   last_success
   last_failure
   last_test
   success_rate
   latency_ms
   capabilities
   notes
   error_category
   timeout_behavior
   rate_limit_behavior

4. Do NOT recreate the database.
5. Do NOT alter unrelated tables.
6. Do NOT change Telegram behavior.
7. Do NOT implement provider discovery/routing/scoring/cooldown yet.
8. Preserve existing public functions/classes.

9. After duplicate merge, create a SQLite UNIQUE index on:
   provider, model, endpoint

10. Make the registry upsert use the unique identity and prevent future duplicates.
    Incoming NULL values must not erase existing non-null values.

11. Immediately run:
    - PRAGMA integrity_check
    - PRAGMA foreign_key_check
    - duplicate-group query
    - registry row count
    - unique-index verification
    - Python compile checks

12. Create:
    logs/dynamic-ai-stage1-report-${STAMP}.txt

The report must contain:
IMPLEMENTED
BACKUP
REGISTRY_BEFORE
REGISTRY_AFTER
DEDUPLICATION
UPSERT
DATABASE
TESTED
WARNINGS
REMAINING
FINAL_STATUS

At the end print exactly:
STAGE1_FINAL_STATUS=COMPLETE
or
STAGE1_FINAL_STATUS=COMPLETE_WITH_WARNINGS
or
STAGE1_FINAL_STATUS=BLOCKED

IMPORTANT:
MAKE THE CODE CHANGES NOW.
Do not spend time explaining what you would do.
Do not merely produce a plan.
Do not wait for confirmation.
STOP immediately after Stage 1 verification.
